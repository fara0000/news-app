import EStyleSheet from 'react-native-extended-stylesheet';
import { moderateScale, scale, } from 'react-native-size-matters';
import { COLORS } from "../../constants";

export const styles = (headerBackground) => EStyleSheet.create({

},);